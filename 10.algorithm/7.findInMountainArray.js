/**
 * 山脉数组中查找目标值
 * @param target
 * @param mountainArr
 * case1: array = [1,2,3,4,5,3,1], target = 3 => 2
 * case2: array = [0,1,2,4,2,1], target = 3 => -1
 */

let arr = [1, 2, 3, 4, 5, 3, 1]

function binarySearch(arr) {

}


let findInMountainArray = function (target, mountainArr) {
    let start = 0
    let end = mountainArr.length - 1


}
