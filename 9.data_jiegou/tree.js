
/**
 * @param text
 * @constructor
 */
function Node(text) {
    this.text = text
    this.left = null  // 节点的左孩子引用
    this.right = null // 节点的右孩子引用
}

let binaryTree = ['a', 'b', 'c', 'd', 'e', 'f', 'h', 'i']

function buildTree(node, i) {

}

